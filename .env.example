# PayHero API (server-side only — never prefix with VITE_)
# Copy to `.env` locally, and set the same keys in Vercel → Project → Settings → Environment Variables.
PAYHERO_API_USERNAME=
PAYHERO_API_PASSWORD=
PAYHERO_CHANNEL_ID=
PAYHERO_AUTH_TOKEN=
